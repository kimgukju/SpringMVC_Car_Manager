<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
 <mapper namespace="com.callor.car.dao.CarDao">
 
 	<select id="findCarSelect" resultType="CarDto">
 	SELECT c_seq, c_division, c_sdate, c_stime,
		c_sdistance, c_adate, c_atime,
		c_adistance, c_place, c_cost
	FROM tbl_car
 	</select>
 	
 	<select id = "insert">
 	INSERT INTO tbl_car (c_division, c_sdistance, c_place) VALUES (1, 1, 1)
 	</select>
 
 
 </mapper> 